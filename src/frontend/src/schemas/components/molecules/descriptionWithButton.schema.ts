import type { DefaultProps } from "../defaultProps.schema";

export interface DescriptionButtonProps extends DefaultProps {
	titleComponent: string;
	classTitle?: string;
	idTitle?: string;
	textComponent: string;
	classText?: string;
	idText?: string;
	textButton: string;
	classButton?: string;
	idButton?: string;
}
